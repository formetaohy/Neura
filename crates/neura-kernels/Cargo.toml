[package]
name = "neura-kernels"
version.workspace = true
edition.workspace = true
description = "Device bodies the megakernel runs for every task kind"
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
neura-abi = { workspace = true }
